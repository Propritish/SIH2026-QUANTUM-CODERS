# MongoDB Atlas connection string — Atlas dashboard > Connect > Drivers
MONGODB_URI=mongodb+srv://projigs98_db_user:Proprit2005@cluster0.tl0w9st.mongodb.net/?appName=Cluster0
MONGODB_DB=odisha_ar_heritage

# Long random string used to sign JWTs — e.g. `openssl rand -hex 32`
JWT_SECRET=replace-with-a-long-random-string

# Local Express server port (used by `npm run server` / `npm run dev:full`)
PORT=5000

# One admin account is seeded from these — see scripts/seed.js.
# Leave blank to skip creating an admin account.
ADMIN_EMAIL=admin@odisha-ar-heritage.dev
ADMIN_PASSWORD=change-this-before-seeding
